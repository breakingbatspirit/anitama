<%= render 'users/header1' ,user:@user %>
<!-- 上半分 -->
<div class="container">

  <div class="upper-container clearfix">
    <h1 class="cds-new-title">商品追加</h1>
    <%= @animes %>

    <div class="upper-cont-left">

      <%= form_for(@cd, url: cds_path) do |f| %>

        <div>
        <!-- <div class="" style="border: 1px solid black; width: 80%; height: auto; margin: 10%"> -->
          <%= image_tag 'cd.jpg', class:"cd-image" %>
            <%#= attachment_image_tag(@cds.user, :profile_image, :fill, 50, 50, fallback: "no_image.jpg") %>
          <!-- <div align="center"> -->
            <%#= attachment_image_tag @user, :image, :fill,200,200, format: 'jpeg', size: "180x180", fallback: "sample-t.png" %>
          <!-- </div> -->
        </div>
      


<div class="container">

    <h1 class="cds-new-title">商品追加</h1>

    <!-- Cdモデル以外に送る右上の部分 -->
    <div class="upper-cont-right">
      <%= render 'add_record', cd: @cd %>
    </div>

    <!-- Cdモデルに送るほか全部 -->
      <%= render 'form', cd: @cd %>
      <%#= link_to 'Back', root_path %>

</div>





<div style="margin: 200px 0; border-top:10px dotted grey">
<!-- (仮)要素が新規追加されたか確認のためのフォーム -->
<div style="margin:20px 20px 0">
  <p>↓（仮）新規追加確認用</p>

  <div class="dropdown">
    <button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown">
      アニメ名を選択
      <span class="caret"></span>
    </button>

    <ul class="dropdown-menu" role="menu">
      <li role="presentation">
        <% if @animes.present? %>
          <% @animes.each do |anime| %>
            <p><%= anime.anime_title %></p>
          <% end %>
        <% end %>
      </li>
    </ul>
  </div>
</div>
<!-- ここまで -->
<!-- (仮)要素が新規追加されたか確認のためのフォーム -->
<div style="margin:20px 20px 0">

  <div class="dropdown">
    <button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown">
      レーベル名を選択
      <span class="caret"></span>
    </button>

    <ul class="dropdown-menu" role="menu">
      <li role="presentation">
        <% if @labels.present? %>
          <% @labels.each do |label| %>
            <p><%= label.label_name %></p>
          <% end %>
        <% end %>
      </li>
    </ul>
  </div>
</div>
<!-- ここまで -->
<!-- (仮)要素が新規追加されたか確認のためのフォーム -->
<div style="margin:20px 20px 0">

  <div class="dropdown">
    <button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown">
      アーティスト名を選択
      <span class="caret"></span>
    </button>

    <ul class="dropdown-menu" role="menu">
      <li role="presentation">
        <% if @artists.present? %>
          <% @artists.each do |artist| %>
            <p><%= artist.artist_name %></p>
          <% end %>
        <% end %>
      </li>
    </ul>
  </div>
</div>
<!-- ここまで -->
</div>